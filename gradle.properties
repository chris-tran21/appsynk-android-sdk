# Project-wide Gradle settings for the AppSynk Android SDK.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# Required: the SDK depends on AndroidX artifacts (WorkManager, security-crypto,
# lifecycle-process, …). Without this, AGP fails dependency resolution.
android.useAndroidX=true
